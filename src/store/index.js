import { def } from "@vue/shared";
import {createStore} from "vuex";

const state={};
const getters={};
const mutations={};
const actions={};

export default createStore({
    state,
    getters,
    mutations,
    actions,
    modules:{},
})